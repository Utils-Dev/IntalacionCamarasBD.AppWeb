
<div class="container mt-4">
    <h2>Listado de Clientes</h2>

    <div class="row mb-3">
        <div class="col-auto">
            <a asp-action="Create" class="btn btn-primary">Crear Nuevo Cliente</a>
        </div>
    </div>
    <div>
        <table class="table table-striped table-bordered" id="tbClientes">
            <thead class="table-dark">
                <tr>
                    <th>Nombre</th>
                    <th>Telefono</th>
                    <th>DUI</th>
                    <th>Correo</th>
                    <th>Fecha de Registro</th>
                    <th>Fecha de Modificación</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @* @foreach (var marca in Model)
                {
                <tr>
                <td>@marca.Id</td>
                <td>@marca.Nombre</td>
                <td>@(marca.Estado == 1 ? "Activo" : "Inactivo")</td>
                <td>
                <a asp-action="Edit" asp-route-id="@marca.Id" class="btn btn-sm btn-warning me-2">Modificar</a>
                <a asp-action="Delete" asp-route-id="@marca.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
                </tr>
                } *@
            </tbody>
        </table>

    </div>

</div>
@section Scripts {
    <script>
        $(document).ready(function () {
            new DataTable('#tbClientes', {
                paging: true,
                searching: false,
                ordering: false,
                lengthChange: false,
                language: {
                    url: '/es-ES.json'
                }
            });
        });
    </script>
}